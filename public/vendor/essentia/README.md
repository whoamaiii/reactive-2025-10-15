Place Essentia WASM build here to enable fully offline BPM/beat-grid analysis.

Expected file:
- essentia-wasm.web.js

Version suggested: 0.1.0 (matching CDN fallback).

The worker will first try this local path before falling back to CDN.


